template:
  name: Checkout Code
  identifier: Checkout_Code
  versionLabel: "0.0.1"
  type: Stage
  projectIdentifier: default_project
  orgIdentifier: default
  spec:
    type: CI
    spec:
      cloneCodebase: false
      infrastructure:
        type: VM
        spec:
          type: Local
          spec:
            os: Linux
      execution:
        steps:
          - step:
              type: Run
              name: Echo Clone Info
              identifier: Echo_Clone_Info
              spec:
                shell: Sh
                command: |
                  echo "hello from CI template"
